namespace Zonit.Dashboard.Extensions;

/// <summary>Which side of the dashboard a drawer extension docks to.</summary>
public enum DrawerAnchor
{
    /// <summary>Left-hand drawer (navigation side by convention).</summary>
    Start,

    /// <summary>Right-hand drawer (extension panels — notifications, tasks, etc.).</summary>
    End,
}
